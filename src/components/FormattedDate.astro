---
interface Props {
	date: string;
}

const { date } = Astro.props;
---

<time>
	{
		new Date(date).toLocaleDateString('uk-UA', {
			year: 'numeric',
			month: 'short',
			day: 'numeric'
		})
	}
</time>
